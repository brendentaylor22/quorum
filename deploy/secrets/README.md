# Runtime secrets

Create `tunnel-credentials.json` here with mode `0400`, owned by deployment administrator. Directory and JSON files are ignored by Git. Never place TMDB credentials here until Phase 3 catalog import exists.
